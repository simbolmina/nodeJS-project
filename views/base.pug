doctype html 
html
    head
        block head 
        meta(charset='UTF-8')
        meta(name='viewport' content='width=device-width, initial-scale=1.0')
        //- <link rel="stylesheet" href="css/style.css"/> html
        link(rel='stylesheet' href='/css/style.css')
        link(rel='shourtcut icon' type='image/png' href='/img/favicon.png')
        link(rel='stylesheet'   href='https://fonts.googleapis.com/css?family=Lato:300,300i,700')
        
        title Natours | #{title}

    body
        // HEADER
        include _header

        // CONTENT
        block content
            h1 This is a placeholder heading

        
        // FOOTER
        include _footer

        //- we moded this here in order to let it load locations so we can use it.
        script(src='https://js.stripe.com/v3/')
        script(script-src-elem='/js/mapbox.js')
        script(src='/js/bundle.js')
        //- script(src='/js/login.js')
        //- script(script-src-elem='https://cdnjs.cloudflare.com/ajax/libs/axios/0.26.1/axios.min.js')